import {BookOpen,ClipboardCheck,LayoutDashboard,Moon,NotebookTabs,Settings,Sun} from 'lucide-react'; import {NavLink,Outlet} from 'react-router-dom'; import {useUI} from '../stores/ui';
const nav=[['/','대시보드',LayoutDashboard],['/certifications','자격증',BookOpen],['/mock-exam','모의고사',ClipboardCheck],['/wrong-notes','오답노트',NotebookTabs],['/admin','관리',Settings]] as const;
export function Layout(){const {dark,toggleDark}=useUI();return <div className={dark?'app dark':'app'}><a className="skip" href="#main">본문 바로가기</a><aside><NavLink to="/" className="brand"><span>CE</span><div><b>CertFlow</b><small>시험 준비의 흐름</small></div></NavLink><nav aria-label="주 메뉴">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'}><Icon size={19}/>{label}</NavLink>)}</nav><button className="theme" onClick={toggleDark}>{dark?<Sun/>:<Moon/>}<span>{dark?'라이트 모드':'다크 모드'}</span></button></aside><div className="shell"><header className="top"><span className="mobile-brand">CertFlow</span><span className="status"><i/> API 연결 모드</span></header><main id="main"><Outlet/></main></div></div>}
